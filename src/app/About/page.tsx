import Section from "@/components/Section";

export const metadata = {
    title: "Nosotros",
  };

export default function About(){
    return (
        <div>
           <Section/>
        </div>
    )
}